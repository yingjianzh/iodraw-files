```mermaid

flowchart TD
    
  
    subgraph Token生成
        A[用户ID] -->|生成Payload| B[JWT Payload]
        B -->|1. Header| C[设置算法与类型-HS256]
        B -->|2. Payload| D[设置声明信息,claim 信息为 user_id:xx, issue_at:xxx]
        B -->|3. Signature| E[签名密钥,access_token和refresh_token的签名秘钥不一样]
        C & D & E -->|组合生成| F[JWT Token]
    end
    
    subgraph Token存储
        F --> G[Redis存储]
        G -->|生成Access Token| H[Access Token]
        G -->|生成Refresh Token| I[Refresh Token]
        
    end

    subgraph Token时效
        T[Token类型] -->|短期Token| M[accessToken - 2小时]
        H -->|长期Token| [refreshToken - 30天固定期限]
    end

```