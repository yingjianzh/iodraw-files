```mermaid
sequenceDiagram
    
    participant 同步队列
    participant app-server
    participant MongoDB

    同步队列->>同步队列: 合并相同data_id操作（保留最高版本）
    同步队列->>app-server: POST /sync-batch
    app-server->>app-server: 
    
    loop 批量写入
        app-server->>MongoDB: 条件更新（version+1）
        alt 更新成功
            MongoDB-->>app-server: 确认+新版本号
        else 版本冲突
            MongoDB-->>app-server: 返回当前版本
        end
    end
    
    app-server-->>同步队列: 批量返回结果
    同步队列->>IndexedDB: 仅保留失败操作
```