```mermaid
sequenceDiagram
    participant A as 设备A(本地v6-7)
    participant S as 服务端(v10)
    participant DB as MongoDB

    %% 初始同步尝试
    A->>S: 提交操作日志(v6)
    S->>DB: 查询当前版本(component/instance/data三元组)
    DB-->>S: 当前版本=10
    S-->>A: 返回409 Conflict

    %% 冲突解决阶段
    A->>S: 请求冲突日志(v6→v10)
    S->>DB: 查询范围日志(v7到v10)
    DB-->>S: 返回日志[v7,v8,v9,v10]
    S-->>A: 返回增量日志

    %% 本地数据重建
    A->>A: 执行操作重放：
    note right of A: 1. 应用服务端v7日志到本地v6数据
    2. 应用v8到本地v7数据
    3. 应用v9到本地v8数据
    4. 应用v10到本地v9数据


    %% 本地操作转换
    A->>A: 转换本地操作：
    

    %% 重新提交
    A->>S: 提交新操作日志(v11)
    S->>DB: 验证版本(v10+1=11)
    DB-->>S: 更新成功(v11)
    S-->>A: 确认同步成功

    A->>S: 提交新操作日志(v12)
    S->>DB: 验证版本(v11+1=12)
    DB-->>S: 更新成功(v12)
    S-->>A: 确认同步成功
```