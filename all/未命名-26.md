```mermaid
sequenceDiagram
    participant A as 设备A(v2)
    participant S as 服务端(v5)
    participant DB as 操作日志存储

    A->>S: 同步请求（携带v2）
    S->>DB: 查询最新连续日志
    alt 存在v3-v5的连续日志
        S-->>A: 返回增量日志[v3,v4,v5]
        A->>A: 合并逻辑：
        1. 应用服务端日志
        2. 重放本地修改
        3. 生成新版本v6
        
        A->>S: 提交合并后数据（base=v5）
        S->>MongoDB: 验证版本连续性（v5→v6）
        S-->>A: 确认更新成功
    else 存在断档日志（如缺少v4）
        S-->>A: 返回数据全量快照（v5）
        A->>A: 丢弃本地修改
        A->>S: 确认版本同步完成
    end
```