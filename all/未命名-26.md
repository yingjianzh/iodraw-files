```mermaid
sequenceDiagram
    participant A as 设备A(本地v6-7)
    participant S as 服务端(v10)
    participant DB as MongoDB

    %% 初始状态
    Note over A: 本地数据状态：\n- 基础版本v5\n- 待同步操作v6(v5→v6)\n- 待同步操作v7(v6→v7)

    %% 同步尝试
    A->>S: 提交操作日志(v6)
    S->>DB: 查询当前版本
    DB-->>S: 当前版本=10
    S-->>A: 返回409 Conflict

    %% 获取增量日志
    A->>S: 请求冲突日志(v6→v10)
    S->>DB: 查询范围日志(v6到v10)
    DB-->>S: 返回日志[v6(v5→v6),v7(v6→v7),v8(v7→v8),v9(v8→v9),v10(v9→v10)]
    S-->>A: 返回增量日志

    %% 关键解决步骤
    A->>A: 分离数据状态：
    Note over A: 1. 基础数据版本v5（原始状态）
    Note over A:2. 服务端日志链v6→v10
    3. 本地待同步操作v6→v7

    A->>A: 重建数据状态：
    1. 从v5开始重放服务端v6→v10日志 → 得到状态v10
    2. 保留本地操作v6→v7（基于v5）

    A->>A: 操作转换（Operational Transformation）：
    1. 将本地v6操作（基于v5）转换为基于v10的操作v11
    2. 将本地v7操作（基于v6）转换为基于v11的操作v12

    A->>A: 冲突检测（字段级）：
    检查转换后的操作与服务端v6→v10日志的字段冲突
    alt 字段无冲突
        A->>A: 自动合并
    else 字段冲突
        A->>A: 保留最新时间戳操作\n或提示用户手动解决
    end

    %% 重新提交
    A->>S: 提交转换后操作[v11,v12]
    S->>DB: 验证版本(v10+1=11)
    DB-->>S: 更新成功(v12)
    S-->>A: 确认同步成功
```