```mermaid
sequenceDiagram
    participant 前端
    participant 同步模块
    participant App-Server
    participant MongoDB

    前端->>同步模块: 发送更新请求
    Note over 前端,同步模块: 包含字段路径和值
    同步模块->>App-Server: {
        "op": "update",
        "id": "rec_001",
        "changes": {
            "title": "新标题",
            "info.address": "新地址"
        }
    }
    App-Server->>MongoDB: 动态构造更新语句
    MongoDB-->>App-Server: 确认更新
    App-Server-->>同步模块: 返回成功
```