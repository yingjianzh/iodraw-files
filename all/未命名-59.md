```mermaid
flowchart TD
    A[开始] --> B{是否需要恢复下载?}
    
    B -->|初始下载| C[发起请求: /api/mode/downloadData?transitionId={id}]
    
    B -->|需要恢复| D{恢复哪种类型?}
    D -->|简单类型| E[发起请求: /api/mode/downloadData?transitionId={id}&type={type}]
    D -->|组件/实例类型| F[发起请求: /api/mode/downloadData?transitionId={id}&type={type}&timestamp={timestamp}]
    
    C --> G[后端处理下载请求]
    E --> G
    F --> G
    
    G --> H[结束]
```