```mermaid
flowchart TD
    A[开始] --> B[调用后端接口设置状态为transition_to_online]
    B --> C[初始化数据导出上下文]
    C --> D[从IndexedDB读取表元数据信息]
    
    D --> E[估算各表数据大小]
    E --> F[确定分块策略:按表分组到合适大小的块]
    
    F --> G[创建manifest.json基本结构]
    G --> H[初始化数据处理进度:0%]
    
    subgraph 数据分块处理
    I[处理下一个表] --> J[从IndexedDB读取当前表数据]
    J --> K[处理数据字段(NumberLong转换等)]
    K --> L[将处理后数据添加到当前块]
    
    L --> M{当前块大小超过阈值?}
    M -->|是| N[完成当前块,计算校验和]
    M -->|否| O{还有更多表要处理?}
    
    N --> O
    O -->|是| I
    O -->|否| P[完成最后一个块,计算校验和]
    end
    
    H --> I
    
    P --> Q[完成manifest文件,添加所有块信息]
    Q --> R[计算整体校验和并添加到manifest]
    
    R --> S[创建ZIP归档文件]
    S --> T[将所有JSON文件添加到ZIP]
    
    T --> U[监控进度并更新UI:75%]
    U --> V[上传ZIP文件到服务器]
    
    V --> W{上传成功?}
    W -->|否| X[重试上传(最多3次)]
    X --> Y{重试成功?}
    Y -->|否| Z[显示错误,调用取消切换接口]
    Y -->|是| AA[等待后端处理结果]
    
    W -->|是| AA
    AA --> AB{处理成功?}
    AB -->|是| AC[清理临时文件]
    AC --> AD[更新本地状态为在线模式]
    AB -->|否| AE[显示错误,取消切换]
    
    AD --> AF[完成]
    Z --> AF
    AE --> AF
```