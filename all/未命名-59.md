```mermaid
flowchart TD
    A[开始] --> B[调用后端接口设置状态为transition_to_online]
    B --> C[初始化数据导出上下文]
    C --> D[从IndexedDB读取表元数据信息]
    
    D --> E[估算各表数据大小]
    E --> F[确定分块策略:按表分组到合适大小的块]
    
    F --> G[创建manifest.json基本结构]
    G --> H[初始化数据处理进度:0%]
    
    subgraph 数据分块处理
    I[处理下一个表] --> J[从IndexedDB读取当前表数据]
    J --> K[处理数据字段(NumberLong转换等)]
    K --> L[将处理后数据添加到当前块]
    
    L --> M{当前块大小超过阈值?}
    M -->|是| N[完成当前块,计算校验和]
    M -->|否| O{还有更多表要处理?}
    
    N --> O
    O -->|是| I
    O -->|否| P[完成最后一个块,计算校验和]
    end
    
    
```