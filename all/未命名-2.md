```mermaid
sequenceDiagram
    participant Client as 客户端
    participant AuthService as AuthService
    participant Redis as Redis
    participant MongoDB as MongoDB
    participant CryptoServer as CryptoServer

    Client->>AuthService: POST /login (LoginReq)
    AuthService->>Redis: GetNonce(address)
    Redis-->>AuthService: nonce
    alt nonce不匹配
        AuthService-->>Client: 返回 HTTP 401 (NonceMismatch)
    else nonce验证通过
        AuthService->>Redis: DEL "nonce:{address}" 
        AuthService->>AuthService: VerifySignature(address, signature, message)
        alt 签名无效
            AuthService-->>Client: 返回 HTTP 401 (InvalidSignature)
        else 签名有效
            AuthService->>MongoDB: FindByWalletAddress(address)
            alt 用户不存在（首次登录）
                AuthService->>CryptoServer: CreateCaAddress(userID)
                CryptoServer-->>AuthService: caAddress
                AuthService->>MongoDB: 事务操作（用户/设备/积分初始化）
            else 用户存在
                alt 用户被禁用
                    AuthService-->>Client: 返回 HTTP 403 (UserIsDisabled)
                else 正常登录
                    AuthService->>AuthService: generateTokens()
                    AuthService->>AuthService: 生成HttpOnly/Secure Cookie
                    AuthService-->>Client: 返回 HTTP 200 + Set-Cookie Header
                    Note over Client,AuthService: Cookie包含：<br>access_token, refresh_token<br>Domain/Secure/HttpOnly/SameSite=Strict
                end
            end
        end
    end
```