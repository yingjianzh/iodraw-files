```mermaid
flowchart LR
    FE[前端请求] --> GIN[Gin路由]
    GIN --> AUTH[签名验证中间件]
    AUTH --> RL[限流中间件]
    RL -->|读写| REDIS[(Redis)]
    RL --> CACHE[本地规则缓存]
    CACHE -->|缓存未命中| MONGO[(MongoDB)]
    
    ADMIN[管理平台] --> MONGO
    ADMIN --> APP[应用进程]
    APP -->|刷新| REDIS
    APP -->|刷新| CACHE
    
   
    
    style AUTH fill:#ff9800
    style RL fill:#2196f3
    style REDIS fill:#e91e63
    style MONGO fill:#4caf50
```