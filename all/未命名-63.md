```mermaid

flowchart TD
    A[调用数据获取接口] --> B{entity_type为空?}

    B -->|是| K[发送init事件,包含数据类型顺序信息]
    B -->|否| D[从指定entity_type开始]
    
    D --> F{entity_type是否为component或instance?}
    
    F -->|否| G[数据传输顺序=从entity_type后开始的子序列,不包含dan]
    F -->|是| H{timestamp是否为0?}
    
    H -->|是| I[设置当前类型=指定entity_type,timestamp=0,数据类型顺序=从entity_type开始的子序列]
    H -->|否| J[设置当前类型=指定entity_type,timestamp=指定timestamp,数据类型顺序=从entity_type开始的子序列]
    
    G --> K
    I --> K
    J --> K
    
   
    K --> P{当前类型是component或instance,且有timestamp>0?}
    P -->|是| Q[按timestamp>指定值查询并传输数据,直到该类型传输完]
    P -->|否| R[正常传输该类型所有数据]
    
    Q --> T{还有下一个类型?}
    R --> T
    
    T -->|是| U[设置当前类型=下一个类型]
    T -->|否| V[发送end事件]
    
    U --> P
    V --> W[结束]

```