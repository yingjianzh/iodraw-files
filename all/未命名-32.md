```mermaid
graph TD
    subgraph 前端
        A[UI层] -->|监听数据变更| B(数据变更触发器)
        B -->|触发同步| C[同步状态管理器]
        C -->|轮询/WebSocket| D[心跳模块]
        A -->|用户操作| E[数据导入/导出UI]
    end

    subgraph 后端(Golang)
        F[API网关] -->|路由请求| G[业务逻辑层]
        G --> H[数据存储引擎]
        G --> I[云存储适配器]
        G --> J[加密/混淆处理器]
        H -->|存储| K[(本地缓存数据库)]
        I -->|读写| L[第三方云存储]
        J -->|加密/签名| M[安全模块]
        G -->|冲突检测| N[同步日志服务]
        N -->|记录变更| O[(同步日志数据库)]
        D -->|心跳状态| P[设备状态管理器]
    end

    subgraph 用户云存储
        L[Google Drive/OneDrive/iCloud]
    end
```