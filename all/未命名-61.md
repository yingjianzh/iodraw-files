```mermaid
flowchart TD
    A[开始恢复数据] --> B[创建临时工作目录]
    B --> C[解密ZIP文件]
    C --> D[解压文件到临时目录]
    
    D --> E[读取metadata.json]
    E --> F[验证备份文件完整性]
    F --> G{验证通过?}
    G -->|否| H[中止恢复并返回错误]
    
    G -->|是| I[逐个处理数据文件]
    
    subgraph 基础数据恢复
        I --> J1[解析canvas.dat]
        J1 --> K1[直接替换canvas数据]
        
        I --> J2[解析canvas_components.dat]
        J2 --> K2[直接替换canvas_components数据]
        
        I --> J3[解析sidebar_components.dat]
        J3 --> K3[直接替换sidebar_components数据]
        
        I --> J4[解析hash_tables.dat]
        J4 --> K4[直接替换hash_tables数据]
    end
    
    subgraph 组件数据恢复
        I --> L[识别所有组件数据文件]
        L --> M[遍历每个组件数据文件]
        M --> N[解析单个dat文件]
        N --> O[提取header和数据]
        
        O --> P{是否有comp_id?}
        P -->|是| Q1[根据comp_id和table_name定位数据]
        P -->|否| Q2[使用其他标识定位数据]
        
        Q1 --> R[处理单个数据块]
        Q2 --> R
        
        R --> S[直接覆盖/插入数据库]
        S --> T{还有更多组件文件?}
        T -->|是| M
    end
    
    subgraph 实例数据恢复
        T -->|否| U[识别所有实例数据文件]
        U --> V[遍历每个实例数据文件]
        V --> W[解析单个dat文件]
        W --> X[提取header和数据]
        
        X --> Y{是否有comp_id和instance_id?}
        Y -->|是| Z1[根据comp_id、instance_id和table_name定位]
        Y -->|否| Z2[使用其他标识定位数据]
        
        Z1 --> AA[处理单个数据块]
        Z2 --> AA
        
        AA --> AB[直接覆盖/插入数据库]
        AB --> AC{还有更多实例文件?}
        AC -->|是| V
    end
    
    AC -->|否| AD[所有数据处理完成]
    AD --> AE[生
```