```mermaid
flowchart TD
    A[创建者请求销毁应用] --> B[系统验证权限]
    B --> C{验证通过?}
    C -->|否| D[返回错误信息]
    C -->|是| E[更新应用状态为已销毁或未发布]
    E --> F[返回销毁成功响应]
    
    G[用户访问分享链接] --> H[解析shareToken]
    H --> I{Token格式和签名有效?}
    I -->|否| J[显示无效链接页面]
    I -->|是| K[从token解析出应用ID和创建者ID]
    K --> L[查询应用状态]
    L --> M{应用存在且已发布?}
    M -->|否| N[显示"链接已失效"页面]
    M -->|是| O[加载应用体验环境]
```