```mermaid
sequenceDiagram
    participant User as 用户
    participant App as 应用
    participant IndexedDB as 本地数据库
    
    User->>App: 选择备份文件并恢复
    App->>App: 验证备份文件
    App->>User: 确认覆盖本地数据
    User->>App: 确认继续
    
    App->>App: 提取备份数据
    
    App->>IndexedDB: 清除现有数据
    IndexedDB->>App: 确认清除完成
    
    App->>IndexedDB: 存储空间数据
    IndexedDB->>App: 确认存储
    
    App->>IndexedDB: 存储组件数据
    IndexedDB->>App: 确认存储
    
    Note over App,IndexedDB: 处理批量数据
    
    loop 组件级数据恢复
        App->>App: 准备数据批次
        App->>IndexedDB: 存储组件数据批次
        IndexedDB->>App: 确认批次存储
    end
    
    loop 实
```