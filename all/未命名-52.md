```mermaid
flowchart TD
    Start([恢复数据到服务器]) --> PrepareAPISession[准备API会话]
    PrepareAPISession --> SetAuthHeader[设置认证头]
    
    SetAuthHeader --> RequestSnapshot[请求创建服务器快照]
    RequestSnapshot --> SnapshotResponse{快照创建成功?}
    
    SnapshotResponse -->|否| RetrySnapshot{重试?}
    SnapshotResponse -->|是| StoreSnapshotID[存储快照ID]
    
    RetrySnapshot -->|是| RequestSnapshot
    RetrySnapshot -->|否| AbortRestore[中止
```