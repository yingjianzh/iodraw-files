```mermaid
flowchart TD
    Start([处理组件数据]) --> FetchAllCompData[获取所有组件数据]
    FetchAllCompData --> GroupByCompID[按component_id分组]
    GroupByCompID --> InitCompProcess[初始化组件处理]
    
    InitCompProcess --> ProcessNextComp[处理下一个组件]
    ProcessNextComp --> HasMoreComps{还有组件?}
    
    HasMoreComps -->|否| FinishCompProcess[完成组件处理]
    HasMoreComps -->|是| GetCurrentComp[获取当前组件数据]
    
    GetCurrentComp --> GroupByTableName[按table_name分组]
    GroupByTableName --> ProcessCompTables[处理组件表]
    
    ProcessCompTables --> ProcessNextTable[处理下一个表]
    ProcessNextTable --> HasMoreTables{还有表?}
    
    HasMoreTables -->|否| UpdateCompProgress[更新组件处理进度]
    HasMoreTables -->|是| GetCurrentTable[获取当前表数据]
    
    GetCurrentTable --> EstimateTableSize[估算表数据大小]
    EstimateTableSize --> SizeCheck{大小 > 5MB?}
    
    SizeCheck -->|否| ProcessSingleChunk[作为单块处理]
    SizeCheck -->|是| DetermineChunks[确定分块数量]
    
    ProcessSingleChunk --> SaveChunkFile[保存为comp_id-table_name-0.dat]
    DetermineChunks --> ProcessMultiChunks[处理多个数据块]
    
    ProcessMultiChunks --> ChunkLoop[循环处理每个块]
    ChunkLoop --> SaveChunkFiles[保存为
```