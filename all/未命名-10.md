```mermaid
graph TB
    subgraph "App-Backend 消息路由"
        WS_Receiver[WebSocket Receiver<br/>接收LLM事件]
        Router[Message Router<br/>根据session_id路由]
        SSE_Manager[SSE Connection Manager<br/>管理所有SSE连接]
    end
    
    subgraph "存储层"
        SessionStore[(Session Store<br/>session_id -> SSE conn)]
        TaskStore[(Task Store<br/>task_id -> session_id)]
    end
    
    LLM[LLM-Server] -->|WS Event<br/>{session_id, task_id, ...}| WS_Receiver
    WS_Receiver --> Router
    
    Router --> SessionStore
    Router --> TaskStore
    Router --> SSE_Manager
    
    SSE_Manager -->|推送事件| SSE_Conn1[SSE Connection 1]
    SSE_Manager -->|推送事件| SSE_Conn2[SSE Connection 2]
    SSE_Manager -->|推送事件| SSE_Conn3[SSE Connection N]
```