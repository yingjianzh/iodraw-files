```mermaid
graph LR
    A[Task创建] --> B[存入MongoDB]
    B --> C[缓存到Redis<br/>快速查询]
    
    C --> D[Todo执行更新]
    D --> E[更新MongoDB]
    D --> F[更新Redis缓存]
    
    F --> G{Task完成?}
    G -->|是| H[清除Redis缓存]
    G -->|否| C
```

**存储结构**：
```
Redis Key: task:{task_id}
Value: {
  status: "EXECUTING",
  current_todo_index: 2,
  wait_point: {
    wait_id: "wait_xxx",
    type: "SIGN_TX",
    todo_id: "todo_2",
    created_at: timestamp
  }
}
TTL: 1800s
```