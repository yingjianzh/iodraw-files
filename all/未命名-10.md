```mermaid
flowchart TD
subgraph Token时效B -->|短期Token| D[4小时]C -->|长期Token| E[7天]end
subgraph 续期检查F[请求API] -->|携带accessToken| G{检查是否需要续期}G -->|1. 已过期| H[使用refreshToken请求新token]G -->|2. 即将过期| I[后台自动续期]G -->|3. 有效期内| J[正常请求]%% 续期阈值判断K[续期阈值判断]K -->|过期| L[exp < now]K -->|即将过期| M[exp < now + 30分钟]K -->|正常| N[exp > now + 30分钟]end
%% 续期流程H -->|刷新请求| O{验证refreshToken}O -->|有效| P[生成新的accessToken]O -->|无效| Q[要求重新登录]I -->|静默续期| R[生成新accessToken]
```