```mermaid
flowchart TD
    A[API 请求] -->|携带 Token| B[认证中间件]
    
    B -->|1. 提取 Token| C{Token 格式校验}
    C -->|失败| D[返回401]
    
    C -->|成功| E{JWT 签名验证}
    E -->|失败| D
    
    E -->|成功| F{过期检查}
    F -->|已过期| D
    
    F -->|未过期| G{Redis 黑名单检查}
    G -->|在黑名单| D
    
    G -->|验证通过| H[解析用户信息]
    H -->I[注入上下文]
    
    I -->|检查权限| J{权限验证}
    J -->|无权限| K[返回403]
    
    J -->|有权限| 
    
    L --> M[返回响应]
```