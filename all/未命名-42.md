```mermaid
flowchart TD
    subgraph 加密混淆流程
        A[JSON原始数据] --> B[转换为JS模块]
        B --> C[代码混淆]
        C --> D[AES加密]
        D --> E[返回密文]
    end

    subgraph 解密还原流程
        F[接收密文] --> G[AES解密]
        G --> H[执行混淆代码]
        H -->|方法1| I1[直接获取jsonData变量]
        H -->|方法2| I2[正则表达式提取JSON]
        H -->|方法3| I3[直接尝试JSON解析]
        I1 --> J[返回原始JSON数据]
        I2 --> J
        I3 --> J
    end

    E -.-> F
    
    subgraph 详细JS模块结构
        K[var jsonData = {...};]
        L[function getJsonData() { return jsonData; }]
        M[jsonData; // 确保返回值]
    end
    
    B -.-> K
```