```mermaid
sequenceDiagram
    参与者 User as 用户
    参与者 CompTB as 组件表
    参与者 ChangeTB as 变更表
    参与者 SyncSys as 同步系统
    
    User->>CompTB: T1: 修改组件A
    CompTB->>CompTB: T2: 更新组件A的数据和哈希值
    Note over CompTB: 计算并存储新的内容哈希H2
    CompTB->>ChangeTB: T5: 添加变更记录(组件A, 哈希H2)
    
    SyncSys->>ChangeTB: 读取待同步变更记录
    SyncSys->>CompTB: 读取组件A的当前数据
    Note over SyncSys: 计算当前数据的哈希值H2
    SyncSys->>SyncSys: 比较H2和H2
    Note over SyncSys: 如果H2=H2,执行同步
    Note over SyncSys: 如果H2≠H2,重新获取最新数据
    SyncSys->>ChangeTB: 更新变更记录状态(已同步)
```