```mermaid
flowchart TD
    A[用户选择从云端恢复] --> B[开始从云端恢复数据]
    B --> C[验证用户身份]
    
    C --> D[检查本地是否存有用户秘钥]
    D --> E{本地是否有秘钥}
    
    E -->|有| F1[使用本地存储的秘钥]
    E -->|无| F2[提示用户输入秘钥]
    F2 --> F7[安全存储秘钥到本地]
    
    F1 --> G[获取加密的云端数据概览app_state.json文件]
    F7 --> G
    
    G --> H[解密app_state.json文件]
    H --> J[根据app_state.json文件内容,检查本地数据状态]
    J --> K{本地是否存在数据}
    
    K -->|否| L1[下载加密的云端数据]
    K -->|是| L2[检测本地与云端数据冲突]
    
    L2 --> M{是否存在冲突}
    M -->|否| L1
    M -->|是| N[解密并显示冲突详情]
    
    N --> O{用户冲突处理选择}
    O -->|使用云端数据| P1[备份本地数据]
    O -->|合并数据| P2[执行智能合并]
    O -->|选择性恢复| P3[用户手动选择每个冲突项]
    
    P1 --> Q1[清除本地相关数据]
    P2 --> Q2[标记已解决的冲突]
    P3 --> Q3[按用户选择应用数据]
    
    Q1 --> R[下载加密的云端数据]
    Q2 --> R
    Q3 --> R
    
    L1 --> R
    
    R --> S[分批处理加密数据]
    S --> T[使用秘钥解密数据]
    T --> U[反混淆解密后的数据]
    
    U --> V1[处理空间基础数据]
    U --> V2[处理组件数据]
    U --> V3[处理组件实例数据]
    U --> V4[处理空间消息数据]
    
    V1 --> W[将解密后的数据导入到本地数据库]
    V2 --> W
    V3 --> W
    V4 --> W
    
    W --> X[验证数据完整性]
    X --> Y[更新本地同步状态]
    
    Y --> Z{是否有处理失败的部分}
    Z -->|是| AA[记录失败项并提示用户]
    Z -->|否| AB[恢复完成]
    
    AA --> AC[提供重试选项]
    AC --> AB
```