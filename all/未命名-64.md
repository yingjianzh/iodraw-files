```mermaid
flowchart TD
    A[创建者] -->|创建并发布应用| B[应用仓库]
    B -->|点击分享应用| C[生成体验链接]
    C -->|复制链接| D[分享给他人]
    
    E[用户] -->|打开体验链接| F[应用试用页面]
    F -->|交互与预览| G[用户体验应用]
    G -->|刷新页面| F
    G -->|点击"保留该应用"| H{是否已登录?}
    
    H -->|否| I[登录页面]
    I -->|完成登录/注册| J[保存应用]
    H -->|是| J
    
    J -->|保存至默认空间| K[用户默认空间]
    J -->|同时保存至| L[用户的应用仓库]
    
    subgraph 应用数据
        note1[交互数据临时保存]
        note2[刷新页面数据清除]
        note3[登录后才能保存数据]
    end
```