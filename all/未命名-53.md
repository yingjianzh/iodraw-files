```mermaid
flowchart TD
    Start([恢复组件数据]) --> ScanCompDir[扫描组件数据目录]
    ScanCompDir --> GroupFiles[按组件ID和表名分组文件]
    GroupFiles --> InitCompRestore[初始化组件恢复]
    
    InitCompRestore --> ProcessNextComp[处理下一组件]
    ProcessNextComp --> MoreComps{还有组件?}
    MoreComps -->|否| CompleteRestore[完成恢复]
    MoreComps -->|是| GetCompFiles[获取当前组件文件]
    
    GetCompFiles --> GroupByTable[按表名分组文件]
    GroupByTable --> ProcessTables[处理组件表]
    
    ProcessTables --> ProcessNextTable[处理下一表]
    ProcessNextTable --> MoreTables{还有表?}
    MoreTables -->|否| UpdateCompProgress[更新组件进度]
    MoreTables -->|是| GetTableFiles[获取当前表文件]
    
    GetTableFiles --> SortTableChunks[排序表数据块]
    SortTableChunks --> ProcessTableData[处理表数据]
    
    subgraph TableRestore [表数据恢复]
        TR1[获取下一数据块] --> TR2{还有数据块?}
        TR2 -->|否| TR7[完成表数据恢复]
        TR2 -->|是| TR3[读取数据块文件]
        TR3 --> TR4[解密数据块]
        TR4 --> TR5[解压数据块]
        TR5 --> TR6[恢复数据到目标]
        TR6 --> TR1
    end
    
    ProcessTableData --> TableRestore
    
    TableRestore -->
```