```mermaid
flowchart TD
    Start([开始组件数据处理]) --> FetchData[获取全部组件数据]
    FetchData --> DataEmpty{数据为空?}
    
    DataEmpty -->|是| SkipProcessing[跳过处理]
    DataEmpty -->|否| GroupByCompID[按component_id分组数据]
    
    SkipProcessing --> End([结束])
    
    GroupByCompID --> InitCompIDList[初始化组件ID列表]
    InitCompIDList --> ProcessNextComp[处理下一个组件ID]
    
    ProcessNextComp --> MoreComps{还有组件?}
    MoreComps -->|否| CompleteCompProcessing[组件处理完成]
    MoreComps -->|是| GetCurrentComp[获取当
```