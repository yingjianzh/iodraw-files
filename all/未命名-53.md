```mermaid
flowchart TD
    Start([开始恢复]) --> SelectBackupFile[选择备份文件]
    SelectBackupFile --> ValidateFile[验证备份文件完整性]
    ValidateFile --> FileValid{文件有效?}
    
    FileValid -->|否| NotifyInvalidFile[通知文件无效]
    NotifyInvalidFile --> End([结束])
    
    FileValid -->|是| ReadMetadata[读取元数据]
    ReadMetadata --> CheckMode[检查当前模式]
    CheckMode --> CurrentMode{当前模式}
    
    CurrentMode -->|在线模式| PrepareServerRestore[准备服务器恢复]
    CurrentMode -->|本地模式| PrepareLocalRestore[准备本地恢复]
    
    PrepareServerRestore --> VerifyLogin[验证用户登录]
    VerifyLogin --> LoginValid{登录有效?}
    LoginValid -->|否| RequestLogin[请求用户登录]
    LoginValid -->|是| CheckServerSpace[检查服务器空间]
    
    RequestLogin --> UserLogin[用户登录]
    UserLogin --> CheckServerSpace
    
    CheckServerSpace --> ServerReady{服务器就绪?}
    ServerReady -->|否| NotifyServerIssue[通知服务器问题]
    ServerReady -->|是| ConfirmServerRestore[确认服务器恢复]
    
    PrepareLocalRestore --> CheckLocalDB[检查本地数据库]
    CheckLocalDB --> LocalDBReady{本地数据库就绪?}
    LocalDBReady -->|否| PrepareLocalDB[准备本地数据库]
    LocalDBReady -->|是| ConfirmLocalRestore[确认本地恢复]
    
    PrepareLocalDB --> ConfirmLocalRestore
    
    ConfirmServerRestore --> UserConfirm1{用户确认?}
    ConfirmLocalRestore --> UserConfirm2{用户确认?}
    
    UserConfirm1 -->|否| CancelRestore1[取消恢复]
    UserConfirm1 -->|是| ExtractBackup[提取备份文件]
    
    UserConfirm2 -->|否| CancelRestore2[取消恢复]
    UserConfirm2 -->|是| ExtractBackup
    
    CancelRestore1 --> End
    CancelRestore2 --> End
    
    ExtractBackup --> ProcessTables[处理表数据]
    
    subgraph TableRestore [表恢复流程]
        A[获取下一个表] --> B{还有表需处理?}
        B -->|否| CompleteTableRestore[完成表恢复]
        B -->|是| C[读取表元数据]
        
        C --> D{分块表?}
        D -->|是| E[处理分块表]
        D -->|否| F[处理整表]
        
        subgraph ChunkRestore [分块恢复]
            E1[获取下一个块] --> E2{还有块?}
            E2 -->|否| ChunkRestoreComplete[完成块恢复]
            E2 -->|是| E3[读取块数据]
            E3 --> E4[验证块完整性]
            E4 --> E5{块完整性验证通过?}
            E5 -->|否| E6[重新读取块]
            E5 -->|是| E7[恢复块数据]
            E
```