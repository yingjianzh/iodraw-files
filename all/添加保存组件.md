```mermaid
flowchart TD
    A([开始]) --> B{option=0}
    B -- Y --> C[组装LLM Server接口数据,调用LLM Server接口]
    B -- N --> D{是保存组件?option=3}
    E -- N --> L{是保存并添加组件响应数据}
    E -- Y --> F[组装保存组件返回信息,生成message]
    C --> E{是保存组件响应数据}
    L -- N --> R[生成message]
    L -- Y --> K
    D -- Y --> F
    D -- N --> J{是保存并添加组件? \noption=4}
    J -- Y --> K[组装保存并添加组件返回信息,生成message]
    K --> G
    J -- N --> I
    F --> G[保存至t_messages]
    G --> H[流式返回前端]
    H --> I([结束])
```