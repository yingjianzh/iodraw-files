```mermaid
flowchart TD
    A([开始]) --> B{入参action_option=0}
    B -- Y -- 
    B -- Y --> C[组装LLM Server接口数据,调用LLM Server接口]
    C --> E{响应数据是保存组件数据 \n 操作码=1}
    B -- N --> D{是保存组件? \n action_option=1}
    D -- N --> J{是保存并添加组件? \n action_option=2}

    E -- N --> L{是保存并添加组件响应数据}
    E -- Y --> F[组装保存组件返回信息,生成message]
    
    L -- N --> R[构建请求生成message和响应message]
    R --> G
    L -- Y --> K
    D -- Y --> F
    
    J -- Y --> K[组装保存并添加组件返回信息,生成message]
    K --> G
    J -- N --> R
    F --> G[保存至t_messages]
    G --> H[流式返回前端]
    H --> I([结束])
```